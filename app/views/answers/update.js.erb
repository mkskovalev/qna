$('.answer-edit-errors-' + '<%= @answer.id %>').html("<%= render 'shared/errors', resource: @answer %>")

<% if !@answer.errors.present? %>
  $('#answer-' + "<%= @answer.id %>").replaceWith("<%= j render 'answer_simple', answer: @answer %>")
<% end %>
