$('.question-errors').html("<%= render 'shared/errors', resource: @question %>")

<% if !@question.errors.present? %>
  $('.question').html('<%= j render partial: "question_simple", object: @question %>')
<% end %>
